// This is the bootloader scene that loads all the assets for the game

export default class BootLoader extends Phaser.Scene {
    constructor() {
        super({ key: 'BootLoader' });
    }

    preload() {
        // Load nessary assets for the bootloader
    }

}

