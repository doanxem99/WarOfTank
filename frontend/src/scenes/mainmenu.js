// Main menu is the first screen the user sees
// It contains the title, a start button, and a settings button

export default class MainMenu extends Phaser.Scene {
    constructor() {
        super({ key: 'MainMenu' });
    }

}